define({
    depthSortingMask: parseInt('10',2), //bitmask, bit per layer
    layerStrokeMask: parseInt('10',2), //bitmask, bit per layer
    layerClearMask: parseInt('11',2), //bitmask, bit per layer
    layersCount: 2,
    useOctree: false,
    renderOctree: false,
    debug: true
});
