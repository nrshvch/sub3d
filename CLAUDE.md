# CLAUDE.md

Project guidance lives in `AGENTS.md`, so every agent reads the same file. Claude Code loads this
file and pulls that one in via the import below; edit `AGENTS.md`, not this.

@AGENTS.md
